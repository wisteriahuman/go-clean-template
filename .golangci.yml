version: "2"

linters:
  enable:
    - errcheck
    - wrapcheck
    - errorlint
    - gocritic
    - staticcheck
    - govet
    - revive
    - misspell
    - godot
    - gosec
  settings:
    godot:
      scope: declarations
    gocritic:
      enabled-tags:
        - performance
        - style
        - diagnostic
    revive:
      rules:
        - name: exported
    wrapcheck:
      ignoreSigRegexps:
        - \.Errorf\(
    gosec:
      excludes:
        - G304

formatters:
  enable:
    - gofmt
    - goimports
